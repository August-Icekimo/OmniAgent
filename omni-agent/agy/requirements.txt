# AGY specialist agent 容器依賴。
# Task 2：ADK 2.0 自建 specialist agent，透過 ADK 原生 A2A 橋（to_a2a）暴露。
#
# ⚠️ 版本配對（2026-06-23 實證）：google-adk 2.3.0 的 [a2a] extra 釘 a2a-sdk>=0.3.4,<0.4。
# a2a-sdk 1.1.0（最新）改寫了 server API（移除 a2a.server.apps），ADK 的 to_a2a 尚未跟進，
# 故必須用 ADK 釘住的 0.3.x，不可用 1.x。
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.11.0
google-adk[a2a]>=2.3,<3
# [http-server] extra 提供 sse-starlette（JSONRPCApplication 必需）
a2a-sdk[http-server]>=0.3.4,<0.4
google-genai>=1.0
