services:
  postgres:
    image: docker.io/pgvector/pgvector:pg18-trixie
    container_name: omni-agent-postgres-1
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-omni}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: ${POSTGRES_DB:-omni_agent}
      TZ: Asia/Taipei
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ./memory/postgres:/var/lib/postgresql
      - ./db/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-omni} -d ${POSTGRES_DB:-omni_agent}" ]
      interval: 3s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  gateway:
    build:
      context: ./gateway
    container_name: omni-agent-gateway-1
    env_file:
      - .env
    ports:
      - "${GATEWAY_PORT:-8086}:8086"
    depends_on:
      postgres:
        condition: service_started
    restart: unless-stopped
    environment:
      TZ: Asia/Taipei
    volumes:
      - omni-workspace:/workspace

  brain:
    build:
      context: ./brain
    container_name: omni-agent-brain-1
    env_file:
      - .env
    ports:
      - "${BRAIN_PORT:-8000}:8000"
    volumes:
      - whisper-models:/root/.cache/huggingface
      - ./SOUL.md:/SOUL.md:ro
      - omni-workspace:/workspace
      # 終端機 log 共享 volume：sandbox 寫、brain 讀（並負責 7 天保留期清理，
      # 清理需刪檔故掛 rw；brain 為可信服務，rw 不增實質風險）
      - terminal-logs:/terminal-logs
      # A2A 產物交換共享 volume：brain 寫長輸入、讀回大產物（與 agy 同掛，提案 D2）
      - a2a-workspace:/a2a-workspace
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    environment:
      TZ: Asia/Taipei
      # 終端機技能：brain 透過此內網位址呼叫沙箱 exec 服務
      SANDBOX_URL: http://sandbox:8000
      # 終端機 viewer：log 共享 volume 在 brain 容器內的掛載路徑
      TERMINAL_LOG_DIR: /terminal-logs
      # A2A specialist 委派：brain 經此內網位址呼叫 AGY 容器（提案 D2）
      AGY_A2A_URL: http://agy:8000
      # 與 AGY 共享的產物交換目錄（brain 端掛載路徑）
      A2A_WORKSPACE: /a2a-workspace

  skills:
    build:
      context: ./skills
    container_name: omni-agent-skills-1
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_started
    restart: unless-stopped

  searxng:
    image: docker.io/searxng/searxng:latest
    container_name: omni-agent-searxng-1
    # 僅 compose 內網可達，不發布 host port（limiter 因此可關閉）
    volumes:
      - ./searxng/settings.yml:/etc/searxng/settings.yml:ro
    environment:
      SEARXNG_SECRET: ${SEARXNG_SECRET:-omni-agent-searxng-secret}
      TZ: Asia/Taipei
    restart: unless-stopped

  sandbox:
    build:
      context: ./sandbox
    container_name: omni-agent-sandbox-1
    # 終端機技能的執行環境：僅 compose 內網可達，不發布 host port。
    # 刻意不掛 .env（隔絕 secret，達成天然 env 刮除）。
    environment:
      TZ: Asia/Taipei
      # 原始 ANSI log 落地根目錄（共享 volume，供 brain viewer 讀取）
      LOG_ROOT: /sandbox/logs
    # 檔案系統唯讀，僅 /sandbox/work 與 tmpfs 可寫
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - sandbox-work:/sandbox/work
      # 終端機 log 共享 volume：read_only 根 fs 不影響 volume 掛載點可寫
      - terminal-logs:/sandbox/logs
    # 資源限制：防失控命令拖垮主機
    mem_limit: 512m
    pids_limit: 256
    # cpus: 1.0 設定 CFS 硬上限（cpu.max），在 rootless podman→docker-compose
    # 相容路徑下會回報 "NanoCPUs can not be set"。改用相對權重 cpu_shares
    # （映射至 cpu.weight），不需 CFS quota，rootless cgroup v2 必定可用。
    cpu_shares: 1024
    # 縮小攻擊面：禁止提權
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped

  agy:
    build:
      context: ./agy
    container_name: omni-agent-agy-1
    # A2A specialist agent runtime：僅 compose 內網可達，不發布 host port。
    # 沿用 sandbox 隔離模式 — 刻意不掛 .env（隔絕 DB credential / OAuth token），
    # 僅注入單一 secret（AGY 專用 API Key，與 Cindy 日常 OAuth 計費分離，提案 D3）。
    environment:
      TZ: Asia/Taipei
      # 唯一注入的 secret：來源為 host 的 AGY_GEMINI_API_KEY，容器內以 SDK 慣用
      # 的 GEMINI_API_KEY 名稱呈現。AGY 永遠看不到 Cindy 的 GEMINI_API_KEY。
      GEMINI_API_KEY: ${AGY_GEMINI_API_KEY:-}
      # 共享產物目錄（agy 端掛載路徑）
      A2A_WORKSPACE: /a2a-workspace
    # 檔案系統唯讀，僅 /tmp（tmpfs）與 /a2a-workspace（volume）可寫
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      # A2A 產物交換共享 volume：read_only 根 fs 不影響 volume 掛載點可寫
      - a2a-workspace:/a2a-workspace
    # 資源限制：防失控 agent 拖垮主機（對齊 sandbox）
    mem_limit: 2g
    pids_limit: 256
    # 相對權重（映射 cpu.weight），rootless cgroup v2 必定可用（見 sandbox 註解）
    cpu_shares: 1024
    # 縮小攻擊面：禁止提權
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped

volumes:
  terminal-logs:
  omni-workspace:
  sandbox-work:
  whisper-models:
  a2a-workspace:
